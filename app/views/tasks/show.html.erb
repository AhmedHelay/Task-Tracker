<p>
  <strong>Project:</strong>
  <%= Project.find(@task.project_id).name%>
</p>

<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>Status:</strong>
  <%= @task.status %>
</p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<p>
  <strong>Deadline at:</strong>
  <%= @task.deadline_at %>
</p>
<%= render partial: 'comments/new' %>
<%= render partial: 'comments/index' , comment: @comment %>
<br>
<%= link_to 'Edit Task', edit_task_path(@task), class: 'btn btn-primary'%>
<%= link_to 'Back', tasks_path , class: 'btn btn-primary' %>