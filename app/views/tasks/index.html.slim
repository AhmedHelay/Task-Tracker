h1 Tasks
table.table.table-responsive.table-light.table-striped.table-bordered
  thead.thead-light
    tr
      th  Title
      th  Description
      th  Deadline at
      th  Status
      th[colspan="2"]
  tbody
    - @tasks.each do |task|
      - if task.project_id == Project.find(task.project_id).id
        tr
          td
            = link_to task.title , task
          td
            = task.description
          td
            = task.deadline_at
          td
            - if task.status == true
              |  Done 
            - else
              |  On Progress 
          td
            = link_to 'Edit', edit_project_path(task) ,class: 'btn btn-primary'
          td
            = link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } , class: 'btn btn-danger'
br
= link_to 'New Task', new_task_path ,class: "btn btn-primary"