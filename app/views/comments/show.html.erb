<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= User.find(@comment.user_id ).fullname%>
</p>

<p>
  <strong>Task:</strong>
  <%= Task.find(@comment.task_id ).title%>
</p>

<p>
  <strong>Content:</strong>
  <%= @comment.content %>
</p>

<%= link_to 'Edit', edit_comment_path(@comment) ,class: 'btn btn-primary' %>
<%= link_to 'Back', comments_path ,class: 'btn btn-secondary'%>
